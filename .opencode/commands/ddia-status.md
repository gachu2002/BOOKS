---
description: Show DDIA audit status rows
agent: ddia-reader
subtask: true
---
Read only `@learning-marketplace/docs/ddia-status.md`.

Requirements:
- Do not inspect code, the book, or any other docs.
- If `$ARGUMENTS` is empty or `all`, return the full table.
- If `$ARGUMENTS` is a chapter number, return the table header and that row only.
- Return a markdown table only.
