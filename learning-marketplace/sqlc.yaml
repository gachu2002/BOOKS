version: "2"

sql:
  - engine: postgresql
    schema:
      - db/migrations
    queries:
      - db/queries
    gen:
      go:
        package: store
        out: internal/store
        sql_package: database/sql
        emit_interface: true
        emit_json_tags: true
        emit_empty_slices: true
