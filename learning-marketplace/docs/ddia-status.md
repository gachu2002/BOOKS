| Ch | Title | Target | Grade | Verdict | Learn Now? | Strongest Evidence | Weakest Gap | Smallest Next Fix | Verify | Updated |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | Reliable, Scalable, and Maintainable Applications | `A` | `B+` | `needs work` | `yes` | `docs/system.md` and `docs/invariants.md` define the real marketplace as a Postgres system of record with derived projection and analytics paths, while `internal/store/checkout_integration_test.go`, `internal/projector/user_library.go`, `internal/analytics/reports.go`, and `cmd/api/main.go` make reliability, eventual read models, and operability visible in running code | explicit SLOs, retention/delete mechanics, and deployment trade-off drills are still thin, so maintainability and operability are learned more from structure than from repeated operational evidence | add one real retention/export or operational target on an existing path instead of expanding chapter notes | `go test ./internal/store -run Checkout`; `go test ./internal/projector ./internal/analytics`; `go run ./cmd/api`; `GET /healthz`; `GET /readyz`; `GET /v1/users/{userID}/library?source=truth`; `GET /v1/users/{userID}/library?source=projection` | `2026-04-14` |
| 2 | Data Models and Query Languages | `A` | `B+` | `needs work` | `yes` | normalized marketplace tables in `db/migrations/001_initial_schema.sql`, join-backed truth reads in `internal/store/entitlements.go`, JSONB product metadata in `internal/store/types.go`, and outbox-to-projection reshaping in `internal/projector/user_library.go` and `internal/analytics/reports.go` let learners compare normalized operational reads with derived read models and analytics reshaping on the real app | graph queries, GraphQL-style traversal, and DataFrame-oriented analysis are not part of the main product path, and JSON document modeling is still limited to `products.metadata` | add one real feature that depends on richer product metadata or related-product links instead of adding a chapter-only graph/doc demo | `go test ./internal/projector ./internal/analytics`; `GET /v1/users/{userID}/library?source=truth`; `GET /v1/users/{userID}/library?source=projection` | `2026-04-14` |
| 3 | Storage and Retrieval | `A` | `B+` | `needs work` | `yes` | `db/migrations/005_product_search_and_pagination.sql`, `internal/store/products.go`, `internal/httpapi/products.go`, and `internal/store/products_integration_test.go` show real multicolumn and full-text indexes, ranked retrieval, and stable keyset pagination, while `db/migrations/004_batch_reports.sql` plus `internal/analytics/reports.go` make OLTP-versus-derived analytical storage visible in the main project | storage-engine internals (B-trees/LSM), vector search, and true column-store execution are still learned mostly from surrounding architecture rather than directly from the main product path | add one real catalog/search path whose behavior depends on index choice or ranking trade-offs instead of expanding standalone storage labs | `go test ./internal/store -run 'TestSearchPublishedProducts_ReturnsMatches|TestListPublishedProductsByCursor_UsesStableKeysetPagination'`; `go test ./internal/analytics`; `GET /v1/products/published`; `GET /v1/products/search?q=distributed` | `2026-04-14` |
| 4 | Encoding and Evolution | `A` | `B+` | `needs work` | `yes` | `internal/httpapi/helpers.go` enforces strict JSON request decoding, `db/migrations/001_initial_schema.sql` plus `internal/store/types.go` keep flexible JSONB product metadata in the real schema, and `internal/store/checkout.go` with `internal/projector/user_library.go` writes versioned outbox payloads while the projector remains backward-compatible with legacy payloads during replay | the main path still teaches encoding mostly through JSON over HTTP/Postgres/outbox boundaries; there is no binary schema format, schema registry, or public API version negotiation exercise yet | evolve one existing public contract or second real event payload compatibly so learners see client/server or producer/consumer schema change beyond JSON-only paths | `go test ./internal/projector -run TestUserLibraryProjector_ProcessPendingAndRebuild`; `go test ./internal/store -run TestCheckoutCohort_IsIdempotentForSameKey`; `POST /v1/checkouts/live-cohorts` | `2026-04-14` |
| 5 | Replication | `A` | `B+` | `needs work` | `yes` | `docker-compose.replication.yml` provisions a real Postgres primary/replica pair, `internal/config/config.go`, `cmd/api/main.go`, and `internal/app/app.go` wire a separate reader store, and `internal/httpapi/entitlements.go` exposes the same entitlement read through primary (`consistency=strong`) or replica (`consistency=eventual`) paths with the chosen source made explicit in the response | the strict product path still does not teach follower catch-up internals, failover, quorum, or multi-leader/leaderless replication; replica lag has to be induced operationally rather than by a built-in product flow | add one real replica-lag drill around checkout-to-entitlements or checkout-to-library on the existing API path without introducing chapter-only endpoints | `docker compose -f docker-compose.replication.yml up -d`; `go run ./cmd/api`; `POST /v1/checkouts/live-cohorts`; `GET /v1/users/{userID}/entitlements?consistency=strong`; `GET /v1/users/{userID}/entitlements?consistency=eventual` | `2026-04-14` |
| 6 | Partitioning | `A` | `B` | `needs work` | `yes, for user-shard routing` | `cmd/api/main.go` can open multiple configured Postgres shard stores, `internal/store/user_shards.go` now routes real user-scoped reads to an owning store, `internal/httpapi/entitlements.go` and `internal/httpapi/library.go` expose the chosen route and shard owner, and `internal/store/user_shards_integration_test.go` proves entitlement and projection reads come back from different owning stores rather than only returning abstract routing metadata | the repo still does not teach shard rebalancing, hot-partition relief, or local-vs-global secondary-index trade-offs on a real write path, and checkout itself still commits through one primary even when reads are shard-aware | move one existing write or search path onto real shard-local state so learners can inspect repartitioning or secondary-index trade-offs without introducing chapter-only infrastructure | `go test ./internal/store -run 'TestRouteUser_UsesStableVirtualShard|TestCheckoutPlacement_ExposesUserAndCohortRouting|TestUserShards_RoutesUserReadsToOwningStore|TestUserShards_RoutesProjectionReadsToOwningStore'`; `GET /v1/users/{userID}/entitlements`; `GET /v1/users/{userID}/library`; `POST /v1/checkouts/live-cohorts` | `2026-04-14` |
| 7 | Transactions | `A` | `B+` | `needs work` | `yes` | `internal/store/checkout.go` keeps lock, validation, payment, entitlement, audit, and outbox writes inside one Postgres transaction, while `internal/store/checkout_integration_test.go` shows oversell prevention, idempotent retries, and promo exhaustion races on the real checkout flow | write skew, phantoms, and true cross-system atomic commit are still indirect in the strict main path | add one second real transaction-sensitive business flow, such as refund or revocation handling, instead of a chapter-only drill | `go test ./internal/store -run 'TestCheckoutCohort_PreventsOversell|TestCheckoutCohort_IsIdempotentForSameKey|TestCheckoutCohort_PromoExhaustionRace'`; `POST /v1/checkouts/live-cohorts` | `2026-04-14` |
| 8 | The Trouble with Distributed Systems | `A` | `B+` | `needs work` | `yes, with care` | replica-aware reads in `internal/httpapi/entitlements.go`, truth-vs-projection reads in `internal/httpapi/library.go`, lag-producing projection code in `internal/projector/user_library.go`, and the new freshness query in `internal/store/projection.go` expose `pending_events`, `latest_source_event_at`, and `oldest_pending_event_at` on the real projection path so stale-but-valid reads are directly inspectable instead of inferred | clocks, process pauses, delayed packets, and lease-expiry failure handling are still learned mostly from surrounding architecture and support code rather than a core user-facing path | add the same freshness/lag signal to replica-backed entitlements or a real stale-holder drill on an existing worker path | `go test ./internal/projector -run TestUserLibraryProjector_ProcessPendingAndRebuild`; `go test ./internal/store -run TestUserShards_RoutesProjectionReadsToOwningStore`; `GET /v1/users/{userID}/library?source=truth`; `GET /v1/users/{userID}/library?source=projection` | `2026-04-14` |
| 9 | Consistency and Consensus | `A` | `B+` | `needs work` | `with care` | `internal/coordination/lease.go` uses etcd compare-and-put leases plus revision fencing tokens, `cmd/batch-reports/main.go` acquires and renews that lease before a real rebuild, and `internal/analytics/reports.go` persists `rebuilt_by` and `rebuild_fencing_token` in real derived tables so the coordination decision is inspectable instead of hidden in a lab path | the core checkout and API reads still do not depend on consensus-backed coordination, and the repo still lacks a real linearizable ID generator or quorum read/write path in the strict product flow | put the same lease-and-fencing discipline on another real long-running worker such as `cmd/projector`, or add a real failover/stale-holder drill around batch rebuild ownership | `go test ./internal/analytics -run TestReporter_RebuildsDailyRevenueAndCohortFill`; `docker compose -f docker-compose.coordination.yml up -d`; `ETCD_ENABLED=true go run ./cmd/batch-reports` | `2026-04-14` |
| 10 | Batch Processing | `A` | `B+` | `needs work` | `yes` | `cmd/batch-reports/main.go`, `internal/analytics/reports.go`, and `db/migrations/004_batch_reports.sql` rebuild deterministic analytics tables from operational orders and cohorts, while `internal/httpapi/reports.go` and `internal/analytics/reports_test.go` make the derived outputs queryable and repeatable on the real app | the batch path is still single-node Postgres work rather than a distributed filesystem/object-store pipeline, and there is no real workflow orchestration beyond the command entrypoint | add one more real derived report or export path on the same rebuildable batch runner instead of creating a separate batch demo | `go test ./internal/analytics -run TestReporter_RebuildsDailyRevenueAndCohortFill`; `go run ./cmd/batch-reports`; `GET /v1/reports/daily-revenue`; `GET /v1/reports/cohort-fill` | `2026-04-14` |
| 11 | Stream Processing | `A` | `B+` | `needs work` | `yes` | `internal/store/checkout.go` emits real `order.paid` outbox events, `internal/projector/user_library.go` consumes that stream into `user_library_projection`, `cmd/projector/main.go` supports continual consume plus rebuild, and `internal/projector/user_library_test.go` proves incremental processing and replay from the same event log on the real app | the strict product path still lacks broker-backed stream transport, windowed aggregations, and platform-level exactly-once semantics, so the chapter is taught mainly through outbox-driven projections rather than a broader streaming runtime | add one more real outbox consumer or projection freshness signal on an existing path instead of introducing a chapter-only broker demo | `go test ./internal/projector -run TestUserLibraryProjector_ProcessPendingAndRebuild`; `PROJECTOR_MODE=once go run ./cmd/projector`; `PROJECTOR_MODE=rebuild go run ./cmd/projector`; `GET /v1/users/{userID}/library?source=projection` | `2026-04-14` |
| 12 | The Future of Data Systems | `A` | `B+` | `needs work` | `with care` | the real project already combines one operational Postgres write path in `internal/store/checkout.go` with two different derived systems: stream-driven library state in `internal/projector/user_library.go` and batch-rebuilt analytics in `internal/analytics/reports.go`; `internal/store/projection.go`, `internal/httpapi/library.go`, and `internal/httpapi/reports.go` now let learners inspect those derived views and a real projection freshness signal against the source of truth | the strict product path still does not push derived state to clients or integrate an external search/cache sink, so some of DDIA’s broader “dataflow everywhere” philosophy remains architectural direction rather than running product behavior | push the existing projection freshness signal to clients or route one existing derived path into a real search/cache sink | `go test ./internal/projector -run TestUserLibraryProjector_ProcessPendingAndRebuild`; `go test ./internal/analytics -run TestReporter_RebuildsDailyRevenueAndCohortFill`; `GET /v1/users/{userID}/library?source=truth`; `GET /v1/users/{userID}/library?source=projection`; `GET /v1/reports/daily-revenue` | `2026-04-14` |
