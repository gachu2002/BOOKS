| Ch | Title | Related? | DDIA Parts | Project Parts | Read | Run/Inspect | Follow | Limits |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | Reliable, Scalable, and Maintainable Applications | `strong` | reliability, scalability, and maintainability trade-offs; systems of record and derived data; fault-tolerant versus simple architecture | `docs/system.md`; `docs/invariants.md`; `cmd/api/main.go`; `internal/app/app.go`; `internal/projector/user_library.go`; `internal/analytics/reports.go`; `internal/store/checkout_integration_test.go` | `docs/system.md` -> `docs/invariants.md` -> `cmd/api/main.go` -> `internal/app/app.go` -> `internal/projector/user_library.go` -> `internal/analytics/reports.go` | `go test ./internal/store -run Checkout`; `go test ./internal/projector ./internal/analytics`; `go run ./cmd/api`; `GET /healthz`; `GET /readyz` | start with the stated invariants and the one-primary-plus-derived-data architecture, then inspect how the real app keeps correctness-sensitive writes simple while projections and analytics evolve around it | explicit SLOs, retention, and deployment drills remain thin |
| 2 | Data Models and Query Languages | `strong` | relational versus document trade-offs; joins and denormalized read models; many-to-many relationships; derived query views | `db/migrations/001_initial_schema.sql`; `internal/store/types.go`; `internal/store/entitlements.go`; `internal/httpapi/library.go`; `internal/store/projection.go`; `internal/projector/user_library.go`; `internal/analytics/reports.go` | `db/migrations/001_initial_schema.sql` -> `internal/store/types.go` -> `internal/store/entitlements.go` -> `internal/httpapi/library.go` -> `internal/store/projection.go` -> `internal/projector/user_library.go` -> `internal/analytics/reports.go` | `go test ./internal/projector ./internal/analytics`; `GET /v1/users/{userID}/library?source=truth`; `GET /v1/users/{userID}/library?source=projection` | compare the normalized operational schema with join-backed truth reads and denormalized projection/reporting views that reshape the same data for other queries | graph traversals, GraphQL, and DataFrame-first analysis are not part of the real product path |
| 3 | Storage and Retrieval | `strong` | OLTP access patterns; indexes and search; keyset pagination; analytical storage split | `db/migrations/005_product_search_and_pagination.sql`; `internal/store/products.go`; `internal/httpapi/products.go`; `internal/store/products_integration_test.go`; `db/migrations/004_batch_reports.sql`; `internal/analytics/reports.go` | `db/migrations/005_product_search_and_pagination.sql` -> `internal/store/products.go` -> `internal/httpapi/products.go` -> `internal/store/products_integration_test.go` -> `db/migrations/004_batch_reports.sql` -> `internal/analytics/reports.go` | `go test ./internal/store -run 'TestSearchPublishedProducts_ReturnsMatches|TestListPublishedProductsByCursor_UsesStableKeysetPagination'`; `GET /v1/products/published`; `GET /v1/products/search?q=distributed`; `GET /v1/reports/daily-revenue` | inspect how the catalog uses relational indexes for operational reads while analytics tables reshape the same source data for reporting | storage-engine internals, vector search, and true column-store execution are indirect |
| 4 | Encoding and Evolution | `strong` | schema evolution; JSON boundaries; compatibility in events and APIs | `internal/httpapi/helpers.go`; `internal/store/types.go`; `internal/store/checkout.go`; `internal/projector/user_library.go`; `internal/projector/user_library_test.go` | `internal/httpapi/helpers.go` -> `internal/store/types.go` -> `internal/store/checkout.go` -> `internal/projector/user_library.go` -> `internal/projector/user_library_test.go` | `go test ./internal/projector -run TestUserLibraryProjector_ProcessPendingAndRebuild`; `POST /v1/checkouts/live-cohorts` | follow strict HTTP decoding into JSONB storage and versioned outbox payloads, then inspect replay compatibility for legacy projector payloads | binary schema formats and public API version negotiation are absent |
| 5 | Replication | `strong` | single-leader replication; asynchronous replica lag; stronger versus weaker reads | `docker-compose.replication.yml`; `internal/config/config.go`; `cmd/api/main.go`; `internal/app/app.go`; `internal/httpapi/entitlements.go`; `internal/store/entitlements.go` | `docker-compose.replication.yml` -> `internal/config/config.go` -> `cmd/api/main.go` -> `internal/app/app.go` -> `internal/httpapi/entitlements.go` -> `internal/store/entitlements.go` | `docker compose -f docker-compose.replication.yml up -d`; `go run ./cmd/api`; `GET /v1/users/{userID}/entitlements?consistency=strong`; `GET /v1/users/{userID}/entitlements?consistency=eventual` | start the primary/replica pair and compare the same entitlement query through primary and replica-backed paths | failover, quorum, and leaderless internals are not embodied |
| 6 | Partitioning | `strong` | partition keys; virtual shards; request routing to owning nodes; cross-partition risks on user-versus-cohort access paths | `docs/system.md`; `cmd/api/main.go`; `internal/config/config.go`; `internal/store/sharding.go`; `internal/store/user_shards.go`; `internal/store/checkout.go`; `internal/httpapi/entitlements.go`; `internal/httpapi/library.go`; `internal/store/sharding_test.go`; `internal/store/user_shards_integration_test.go` | `docs/system.md` -> `internal/config/config.go` -> `cmd/api/main.go` -> `internal/store/user_shards.go` -> `internal/httpapi/entitlements.go` -> `internal/httpapi/library.go` -> `internal/store/checkout.go` | `go test ./internal/store -run 'TestRouteUser_UsesStableVirtualShard|TestCheckoutPlacement_ExposesUserAndCohortRouting|TestUserShards_RoutesUserReadsToOwningStore|TestUserShards_RoutesProjectionReadsToOwningStore'`; `GET /v1/users/{userID}/entitlements`; `GET /v1/users/{userID}/library`; `POST /v1/checkouts/live-cohorts` | follow configured shard stores from startup wiring into `UserShards.Resolve`, then inspect how user-scoped truth and projection reads hit an owning store while checkout still exposes user/cohort placement and cross-shard risk | shard rebalancing, hot-partition mitigation beyond routing metadata, and global secondary-index trade-offs are still not embodied in the main product path |
| 7 | Transactions | `strong` | ACID boundaries; idempotency; transactional outbox; correctness under concurrency | `docs/invariants.md`; `internal/store/checkout.go`; `internal/store/checkout_integration_test.go`; `internal/httpapi/checkouts.go` | `docs/invariants.md` -> `internal/store/checkout.go` -> `internal/store/checkout_integration_test.go` -> `internal/httpapi/checkouts.go` | `go test ./internal/store -run 'TestCheckoutCohort_PreventsOversell|TestCheckoutCohort_IsIdempotentForSameKey|TestCheckoutCohort_PromoExhaustionRace'`; `POST /v1/checkouts/live-cohorts` | follow the checkout transaction from lock and validation through payment, entitlement, audit, and outbox emission | write skew, phantoms, and true distributed transactions remain indirect |
| 8 | The Trouble with Distributed Systems | `strong` | stale reads from lagging replicas and projections; partial failure made visible to users; observing freshness under delayed processing | `docs/system.md`; `docker-compose.replication.yml`; `internal/httpapi/entitlements.go`; `internal/httpapi/library.go`; `internal/store/projection.go`; `internal/projector/user_library.go` | `docs/system.md` -> `docker-compose.replication.yml` -> `internal/projector/user_library.go` -> `internal/store/projection.go` -> `internal/httpapi/library.go` -> `internal/httpapi/entitlements.go` | `go test ./internal/projector -run TestUserLibraryProjector_ProcessPendingAndRebuild`; `GET /v1/users/{userID}/entitlements?consistency=strong`; `GET /v1/users/{userID}/entitlements?consistency=eventual`; `GET /v1/users/{userID}/library?source=truth`; `GET /v1/users/{userID}/library?source=projection` | compare truth, projection, and replica-backed reads, then inspect the projection freshness metadata to see how delayed-but-valid state is surfaced in the product instead of hidden behind the read model | clocks, process pauses, and network partitions are not exercised directly |
| 9 | Consistency and Consensus | `partial` | leases; fencing tokens; one-owner work; coordination service patterns | `internal/coordination/lease.go`; `cmd/batch-reports/main.go`; `internal/analytics/reports.go`; `internal/httpapi/coordination.go`; `db/migrations/006_batch_report_leases.sql` | `internal/coordination/lease.go` -> `cmd/batch-reports/main.go` -> `internal/analytics/reports.go` -> `internal/httpapi/coordination.go` | `go test ./internal/analytics -run TestReporter_RebuildsDailyRevenueAndCohortFill`; `docker compose -f docker-compose.coordination.yml up -d`; `ETCD_ENABLED=true go run ./cmd/batch-reports` | inspect how etcd-backed lease acquisition yields a fencing token that the real batch rebuild records in derived tables | no core read/write path depends on consensus-backed coordination yet |
| 10 | Batch Processing | `strong` | deterministic rebuilds; derived analytics tables; rebuildable batch outputs | `db/migrations/004_batch_reports.sql`; `internal/analytics/reports.go`; `internal/analytics/reports_test.go`; `cmd/batch-reports/main.go`; `internal/httpapi/reports.go` | `internal/analytics/reports.go` -> `internal/analytics/reports_test.go` -> `cmd/batch-reports/main.go` -> `internal/httpapi/reports.go` | `go test ./internal/analytics -run TestReporter_RebuildsDailyRevenueAndCohortFill`; `go run ./cmd/batch-reports`; `GET /v1/reports/daily-revenue`; `GET /v1/reports/cohort-fill` | follow how operational checkout data is rebuilt into deterministic report tables and then served through the API | there is no distributed filesystem or object-store batch runtime in the strict main path |
| 11 | Stream Processing | `strong` | databases and streams; outbox-driven event streams; incremental projection; replay and rebuild | `internal/store/checkout.go`; `internal/projector/user_library.go`; `internal/projector/user_library_test.go`; `internal/store/projection.go`; `cmd/projector/main.go`; `internal/httpapi/library.go` | `internal/store/checkout.go` -> `internal/projector/user_library.go` -> `internal/projector/user_library_test.go` -> `cmd/projector/main.go` -> `internal/httpapi/library.go` | `go test ./internal/projector -run TestUserLibraryProjector_ProcessPendingAndRebuild`; `PROJECTOR_MODE=once go run ./cmd/projector`; `PROJECTOR_MODE=rebuild go run ./cmd/projector`; `GET /v1/users/{userID}/library?source=projection` | trace the real `order.paid` outbox event into the library projection, then inspect replay and rebuild as two ways of deriving the same state from the event stream | there is no Kafka-style broker, windowing, or stream-stream join runtime |
| 12 | The Future of Data Systems | `strong` | combining specialized tools by deriving data; batch plus stream together; observing derived state; timeliness versus integrity trade-offs | `docs/system.md`; `docs/invariants.md`; `internal/store/checkout.go`; `internal/projector/user_library.go`; `internal/store/projection.go`; `internal/analytics/reports.go`; `internal/httpapi/library.go`; `internal/httpapi/reports.go`; `cmd/projector/main.go`; `cmd/batch-reports/main.go` | `docs/system.md` -> `internal/store/checkout.go` -> `internal/projector/user_library.go` -> `internal/store/projection.go` -> `internal/httpapi/library.go` -> `internal/analytics/reports.go` -> `internal/httpapi/reports.go` | `go test ./internal/projector -run TestUserLibraryProjector_ProcessPendingAndRebuild`; `go test ./internal/analytics -run TestReporter_RebuildsDailyRevenueAndCohortFill`; `GET /v1/users/{userID}/library?source=truth`; `GET /v1/users/{userID}/library?source=projection`; `GET /v1/reports/daily-revenue` | follow one operational write path that fans out into stream-derived library state and batch-rebuilt analytics, then inspect truth, projection, and the projection freshness signal to study timeliness versus integrity in the live product | the repo does not yet push derived state to clients or integrate external derived sinks such as search/cache |
